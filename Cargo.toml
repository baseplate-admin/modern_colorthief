[workspace]
members = [
    "packages/modern-colorthief-python",
    "packages/modern-colorthief-wasm",
]
resolver = "2"


[profile.release.package."*"]
codegen-units = 1
opt-level = 's'
strip = true

[profile.release]
panic = "abort"
codegen-units = 1
lto = "fat"
opt-level = 's'
strip = true
